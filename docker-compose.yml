version: '3.8'

services:
  tradingview-mcp:
    build: .
    container_name: tradingview-mcp
    ports:
      - "8765:8765"
    restart: unless-stopped
    environment:
      - PORT=8765
      - HOST=0.0.0.0
    volumes:
      - ./pinescripts:/app/pinescripts
